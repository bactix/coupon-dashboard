# Loaded automatically by `next build` / `next start` (NODE_ENV=production).
# Backend API base URL for production. No trailing slash.
# NEXT_PUBLIC_* is baked in at build time — rebuild after changing.
NEXT_PUBLIC_API_URL=https://mylebpassbackend-production.up.railway.app
